import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingBottom: 40,
  },
  loading: {
    marginVertical: 20,
  },
  emptySpace: {
    height: 80,
  },
});

export default styles;
