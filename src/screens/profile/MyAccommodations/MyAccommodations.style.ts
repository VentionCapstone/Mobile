import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  scrollContainer: {
    flexGrow: 1,
    padding: 15,
  },
  noAccommodationsText: {
    textAlign: 'center',
    fontSize: 18,
    fontWeight: '300',
  },
});
