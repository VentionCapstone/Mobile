import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 10,
    padding: 15,
  },
  radioContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    height: 60,
    borderRadius: 10,
    paddingHorizontal: 10,
  },
  radioLabel: {
    marginLeft: 8,
  },
});
