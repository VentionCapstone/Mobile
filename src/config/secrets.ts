type SecureStoreKeysProps = {
  ACCESS_TOKEN: string;
  REFRESH_TOKEN: string;
  USER_ID: string;
};

export const SecureStoreKeys: SecureStoreKeysProps = {
  ACCESS_TOKEN: 'accessToken',
  REFRESH_TOKEN: 'refreshToken',
  USER_ID: 'userId',
};
