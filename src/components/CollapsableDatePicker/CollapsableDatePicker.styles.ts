import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  buttonText: {
    fontSize: 15,
    padding: 5,
    justifyContent: 'flex-end',
    textAlign:'right'
  }
});
